{
  "name": "vending",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ vending",
    "create-local": "graph create --node http://localhost:8020/ vending",
    "remove-local": "graph remove --node http://localhost:8020/ vending",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vending"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
